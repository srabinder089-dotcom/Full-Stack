<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Banking Dashboard</title>
    <style>
        :root {
            --primary-color: #4c51bf;
            --secondary-color: #6366f1;
            --text-color: #2d3748;
            --muted-text-color: #718096;
            --bg-color: #f7fafc;
            --card-bg-color: #ffffff;
            --shadow-color: rgba(0, 0, 0, 0.05);
            --border-color: #e2e8f0;
            --font-family: 'Inter', sans-serif;
            --border-radius: 12px;
        }

        body {
            font-family: var(--font-family);
            background-color: var(--bg-color);
            color: var(--text-color);
            margin: 0;
            padding: 2rem;
            line-height: 1.6;
        }

        .container {
            display: grid;
            gap: 2rem;
            grid-template-areas:
                "header"
                "summary"
                "transactions";
            max-width: 1200px;
            margin: 0 auto;
        }

        /* Desktop Layout */
        @media (min-width: 768px) {
            .container {
                grid-template-columns: 1fr 2fr;
                grid-template-areas:
                    "header header"
                    "summary transactions";
            }
        }

        header {
            grid-area: header;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1.5rem;
            background-color: var(--card-bg-color);
            border-radius: var(--border-radius);
            box-shadow: 0 4px 6px var(--shadow-color);
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary-color);
        }

        .user-info {
            display: flex;
            align-items: center;
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            background-color: var(--secondary-color);
            color: white;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            font-weight: 600;
            margin-left: 1rem;
        }

        .dashboard-section {
            background-color: var(--card-bg-color);
            padding: 2rem;
            border-radius: var(--border-radius);
            box-shadow: 0 4px 6px var(--shadow-color);
        }

        .dashboard-section h3 {
            font-size: 1.25rem;
            margin-top: 0;
            margin-bottom: 1.5rem;
            color: var(--text-color);
            font-weight: 600;
        }

        .account-summary {
            grid-area: summary;
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .account-card {
            padding: 1.5rem;
            border: 1px solid var(--border-color);
            border-radius: var(--border-radius);
            background-color: var(--card-bg-color);
        }

        .account-card h4 {
            margin: 0 0 0.5rem;
            color: var(--muted-text-color);
            font-weight: 500;
            font-size: 0.9rem;
        }

        .account-balance {
            font-size: 2.25rem;
            font-weight: 700;
            color: var(--primary-color);
        }

        .transactions {
            grid-area: transactions;
            display: flex;
            flex-direction: column;
        }

        .transaction-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .transaction-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
            border-bottom: 1px solid var(--border-color);
        }

        .transaction-item:last-child {
            border-bottom: none;
        }

        .transaction-info {
            display: flex;
            flex-direction: column;
        }

        .transaction-amount {
            font-weight: 600;
            font-size: 1.1rem;
            color: var(--text-color);
        }

        .transaction-amount.debit {
            color: #e53e3e;
        }

        .transaction-amount.credit {
            color: #38a169;
        }

        .transaction-description {
            font-size: 0.9rem;
            color: var(--muted-text-color);
        }

    </style>
</head>
<body>

    <div class="container">
        <header>
            <div class="logo">MyBank</div>
            <div class="user-info">
                <span>Welcome, Jane Doe</span>
                <div class="user-avatar">JD</div>
            </div>
        </header>

        <section class="dashboard-section account-summary">
            <h3>Account Summary</h3>
            <div class="account-card">
                <h4>Checking Account</h4>
                <div class="account-balance">$5,890.50</div>
            </div>
            <div class="account-card">
                <h4>Savings Account</h4>
                <div class="account-balance">$12,345.00</div>
            </div>
        </section>

        <section class="dashboard-section transactions">
            <h3>Recent Transactions</h3>
            <ul class="transaction-list">
                <li class="transaction-item">
                    <div class="transaction-info">
                        <span class="transaction-amount debit">-$50.00</span>
                        <span class="transaction-description">Netflix Subscription</span>
                    </div>
                    <span class="transaction-date">Mar 25, 2024</span>
                </li>
                <li class="transaction-item">
                    <div class="transaction-info">
                        <span class="transaction-amount credit">+$2,000.00</span>
                        <span class="transaction-description">Salary Deposit</span>
                    </div>
                    <span class="transaction-date">Mar 23, 2024</span>
                </li>
                <li class="transaction-item">
                    <div class="transaction-info">
                        <span class="transaction-amount debit">-$25.75</span>
                        <span class="transaction-description">Groceries at Local Market</span>
                    </div>
                    <span class="transaction-date">Mar 22, 2024</span>
                </li>
            </ul>
        </section>
    </div>

</body>
</html>
